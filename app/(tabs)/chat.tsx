import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const chat = () => {
  return <SafeAreaView className="flex-1 bg-gray-100">chat</SafeAreaView>;
};

export default chat;
